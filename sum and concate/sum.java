class Command 
{
public static void main(String a[])
{
	//int c,x,y;
	//x=Integer.parseInt(a[0]);
	//y=Integer.parseInt(a[1]);
	String c=a[0]+a[1];
	
	System.out.println("The sum is "+c);
}
}
//remove // to for sum or it will concat the to as they are in string
//do string c in // for sum in integer format 